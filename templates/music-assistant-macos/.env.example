# Environment Configuration for Music Assistant (macOS)

# Timezone (use your local timezone)
TZ=America/Los_Angeles

# Base path for Docker volumes (absolute path required, no ~)
BASE_PATH=/Users/mittalmak/docker

# Music Assistant web UI port
MA_PORT=8095
